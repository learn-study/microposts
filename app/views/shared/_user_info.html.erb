<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>

<div class="count_panel">
    <div class="col-md-4">
        ツイート<br>
        <%= link_to current_user.microposts.count, current_user, class: 'big_count' %>
    </div>
    <div class="col-md-4">
        フォロー<br>
        <%= link_to current_user.following_users.count, followings_user_path(current_user), class: 'big_count' %>
    </div>
    <div class="col-md-4">
        フォロワー<br>
        <%= link_to current_user.follower_users.count, followers_user_path(current_user), class: 'big_count' %>
    </div>
</div>